# Resell Brain

The rules and prompts that run Breken's reselling automation, as linked markdown nodes in `Brain/`.
`index.html` renders them as an editable graph (GitHub Pages).

Claude reads these files before every automated run — editing a node changes the automation's behavior.
