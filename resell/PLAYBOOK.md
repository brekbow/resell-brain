# PLAYBOOK → moved to Brain/

**The playbook now lives in the `Brain/` folder** — one markdown file per rule/prompt/workflow stage, linked Obsidian-style.

- **Claude:** read ALL files in `Brain/` before acting. They are the source of truth for pricing, negotiation, autonomy, and safety rules. Start with `Brain/_HOME.md`.
- **Breken:** open `brain.html` in Chrome to see and edit the whole system as a graph. Edits save straight to the Brain files — Claude picks them up on its next run.
